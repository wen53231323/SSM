<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

    <!-- ======================================基本扫描====================================== -->
    <!-- 基本扫描标签方式一 -->
    <context:component-scan
            base-package="Spring.Spring.Controller,Spring.Spring.Service.impl,Spring.Spring.Dao.impl"></context:component-scan>

    <!-- 基本扫描标签方式二 -->
    <context:component-scan base-package="Spring"></context:component-scan>

    <!-- ======================================排除扫描：指定要排除的组件====================================== -->
    <!-- 例：不扫描控制层 -->
    <context:component-scan base-package="Spring">
        <!-- context:exclude-filter标签：排除扫描，指定排除规则 -->
        <!-- type属性：设置排除或包含的依据 -->
        <!--type="annotation"，根据注解排除，expression中设置要排除的注解的全类名-->
        <!--type="assignable"，根据类型排除，expression中设置要排除的类型的全类名-->
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Spring.Controller"/>
    </context:component-scan>

    <!-- ======================================包含扫描：仅扫描指定组件====================================== -->
    <!-- 例：仅扫描控制层 -->
    <!-- use-default-filters属性：取值false表示关闭默认扫描规则 -->
    <!-- use-default-filters="true"：扫描设置的包下的所有类（默认），此时可使用排除扫描 -->
    <!-- use-default-filters="false"：不扫描设置的包下的所有类，此时可使用包含扫描 -->
    <context:component-scan base-package="Spring" use-default-filters="false">
        <!-- context:include-filter标签：包含扫描，指定在原有扫描规则的基础上追加的规则，必须设置use-default-filters="false"-->
        <!-- type属性：设置排除或包含的依据 -->
        <!--type="annotation"，根据注解排除，expression中设置要排除的注解的全类名-->
        <!--type="assignable"，根据类型排除，expression中设置要排除的类型的全类名-->
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Spring.Controller"/>
    </context:component-scan>

</beans>